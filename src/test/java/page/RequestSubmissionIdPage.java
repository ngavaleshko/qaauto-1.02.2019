package page;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.FindBy;

public class RequestSubmissionIdPage {
    private WebDriver driver;

    @FindBy(xpath = "//*[@id=\'newPassword\']")
    private Webdriver newPasswordField;

    @FindBy(xpath =

}
